PRIO HIGH:
- Make sure all settings are assigned a value on startup
 (Move var _Def_seeting to object DEFSET and iterate through components)


PRIO MEDIUM:



PRIO LOW:
- Move div#container.cf colour to darkmode stylesheet, only add seperate style tag when no theme is used

